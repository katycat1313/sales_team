# ── Vertex AI Authentication ────────────────────────────────────
# These packages require Google Cloud CLI to be installed separately:
# Install gcloud: https://cloud.google.com/sdk/docs/install
# Then authenticate: gcloud auth application-default login
google-auth>=2.0.0
google-api-python-client>=2.169.0
google-cloud-aiplatform>=1.72.0

# ── LLM APIs ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.0
google-genai==1.0.0
python-dotenv==1.0.0
httpx==0.27.0
playwright==1.44.0
stripe==10.0.0
gspread>=6.0.0
